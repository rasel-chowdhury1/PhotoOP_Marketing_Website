import ContactUs from "@/components/SiteComponents/ContactUs";
import React from "react";

export const metadata = {
  title: "Contact PhotoOp",
};

const page = () => {
  return (
    <div>
      <ContactUs />
    </div>
  );
};

export default page;
