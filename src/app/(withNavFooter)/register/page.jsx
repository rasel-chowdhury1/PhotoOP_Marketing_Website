import Register from "@/components/Register/Register";
import React from "react";

export const metadata = {
  title: "Snapper Registration",
};

const page = () => {
  return (
    <div>
      <Register />
    </div>
  );
};

export default page;
